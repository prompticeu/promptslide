{
  "name": "powervibe-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "example"
  ],
  "scripts": {
    "dev": "npm run dev -w example",
    "build": "npm run build -w packages/powervibe",
    "dev:example": "npm run dev -w example"
  }
}
